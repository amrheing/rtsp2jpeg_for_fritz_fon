# Example environment configuration for rtsp2jpeg_for_fritz_fon
# Copy to .env and adjust values. DO NOT COMMIT the .env file.

# RTSP Stream Configuration
RTSP_USER=your_username
RTSP_PASS=your_password
RTSP_HOST=192.168.1.100
# RTSP_PATH examples for different camera brands:
# Reolink NVR: /Preview_05_sub (channel 5 substream) or /Preview_05_main (main stream)
# Reolink Camera: /Preview_01_sub or /Preview_01_main
# Hikvision: /Streaming/Channels/101 (main) or /Streaming/Channels/102 (sub)
# Dahua: /cam/realmonitor?channel=1&subtype=0 (main) or subtype=1 (sub)
# Instar: /12 (1080p) or /11 (720p) or /13 (QVGA)
# Foscam: /videoMain or /videoSub
# Axis: /axis-media/media.amp?videocodec=h264
# Ubiquiti UniFi: /s0 (high quality) or /s1 (medium) or /s2 (low)
# TP-Link Tapo: /stream1 (main) or /stream2 (sub)
# Amcrest: /cam/realmonitor?channel=1&subtype=0
RTSP_PATH=/Preview_05_sub

# Default Snapshot Settings
DEFAULT_SNAPSHOT_SLICE=5:3-4

# FritzFon Configuration
FRITZ_ENABLE=true
FRITZ_USER_AGENT=AVM
FRITZ_MAX_WIDTH=640
FRITZ_JPEG_QUALITY=8
FRITZ_CACHE_TTL=3
FRITZ_CACHE_DIR=/tmp
FRITZ_DEFAULT_SLICE=5:3-4

# Performance & Hardware Settings
FAST_MODE=true
FAST_FRAME_CACHE=/var/www/webcam/frame.jpg
FAST_FRAME_MAX_AGE=2
HWACCEL=none
FAST_FALLBACK=true
# PUBLIC_BASE_URL: Base URL for external access (used for Fritz phone network access)
# This is needed when your webcam server is accessed from different networks:
# - Internal LAN: http://192.168.1.100 (same network as camera)
# - Fritz VLAN/Guest network: http://10.0.0.100 (different network interface)
# - External access: http://your-domain.com or http://external-ip
# Set this to the URL that Fritz phones can reach from their network segment
PUBLIC_BASE_URL=http://192.168.1.10

# Optional status endpoint auth token (set to a random string). If empty, status is open.
STATUS_TOKEN=CHANGE_ME_STATUS

